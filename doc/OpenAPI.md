# Documentation OpenAPI

- Swagger UI : `/api/docs/`
- ReDoc : `/api/redoc/`
- Schéma brut : `/api/schema/`

## Export local
```bash
make schema-json   # produit schema.json à la racine
make schema-yaml   # produit schema.yaml à la racine
